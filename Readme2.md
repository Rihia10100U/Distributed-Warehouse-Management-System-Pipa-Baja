Operator mencatat transaksi (bahan baku → WIP → finish good) langsung ke database plant masing-masing melalui Nginx gateway (port 5431/5432). Hub Pusat secara real-time mengagregasi data dari kedua plant menggunakan 6 foreign tables + 3 view UNION ALL. Dashboard Streamlit membaca 3 view tersebut, menyimpan hasil query di Redis, dan menampilkan dalam bentuk tabel/grafik dengan fitur refresh yang menginvalidasi cache.
